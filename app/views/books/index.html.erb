<p style="color: green"><%= notice %></p>

<h1><%= t'.heading' %></h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to t('.show'), book %>
    </p>
  <% end %>
</div>

<%= link_to t('.create'), new_book_path %>
